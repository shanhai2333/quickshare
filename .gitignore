# 运行时数据（文件本体、分片、SQLite）
/data/

# 本地临时/测试目录
/.tmp/

# 编辑器/助手工具的本地工作数据（含本机绝对路径、会话笔记）。
# 它不是项目源码，公开仓库里放这个没意义还会漏本机路径。
/.workbuddy-ai/

# 编译产物
/dist/
/quickshare
/quickshare.exe

# 分享用的源码快照（派生产物，随源码重建，不必入库）。
# internal.zip 是同一类东西——重新打包内容就会变，入库只会让 diff 里
# 混进一大坨二进制。真要分发源码快照，走 Release 的产物。
/go.zip
/internal.zip

# 数据库残留
*.db
*.db-wal
*.db-shm
